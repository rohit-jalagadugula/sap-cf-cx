pipeline {
  agent any
  stages {
    stage('') {
      steps {
        git(changelog: true, url: 'https://github.com/rohit-jalagadugula/sap-cf-cx.git', branch: 'master', poll: true)
      }
    }

  }
}